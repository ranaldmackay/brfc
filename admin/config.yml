backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)

publish_mode: editorial_workflow

media_folder: "build/images/uploads" # Media files will be stored here for processing prior to build
public_folder: "images/uploads" # Processed images are sent here

collections:
  - name: "articles" # Used in routes, e.g., /admin/collections/blog
    label: "Articles" # Used in the UI
    folder: "_posts/articles" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Layout", name: "layout", widget: "hidden", default: "news-article"}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Category", name: "category", widget: "select", options: ["boardroom", "chairmans-chat", "event", "match", "squad"]}
      - {label: "Featured Image", name: "thumbnail", widget: "image", required: false}
      - {label: "Description", name: "description", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}

  - name: "fixtures" # Used in routes, e.g., /admin/collections/blog
    label: "Fixtures" # Used in the UI
    folder: "_fixtures/2017" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Layout", name: "layout", widget: "hidden", default: "news-article"}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Match Date", name: "match-date", widget: "datetime"}
      - {label: "Status", name: "status", widget: "select", options: ["upcoming", "completed", "postponed", "abandoned"], default: "upcoming" }
      - {label: "Home Team", name: "home-team", widget: "string"}
      - {label: "Home Score", name: "home-score", widget: "number", valueType: "int", required: false}
      - {label: "Home Score aet", name: "home-score-aet", widget: "number", valueType: "int", required: false}
      - {label: "Home Score Pens", name: "home-score-pens", widget: "number", valueType: "int", required: false}
      - {label: "Away Team", name: "away-team", widget: "string"}
      - {label: "Away Score", name: "away-score", widget: "number", valueType: "int", required: false}
      - {label: "Away Score aet", name: "away-score-aet", widget: "number", valueType: "int", required: false}
      - {label: "Away Score Pens", name: "away-score-pens", widget: "number", valueType: "int", required: false}
      - {label: "Description", name: "description", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown", required: false}


  - name: "clubs"
    label: "Clubs"
    folder: "_clubs"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
